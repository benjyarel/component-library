import { MenuButton } from "../Menu";
import { Layout, Title } from "./style";

export const Header = () => {
  return (
    <Layout>
      <MenuButton />
      <Title>Component Library</Title>
    </Layout>
  );
};
